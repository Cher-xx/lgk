# 性能审核报告

审核对象：生产构建的本地预览，桌面视口 1440 × 900。

## 结果

| 项目 | 结果 |
| --- | --- |
| 图片数量 | 37 张 |
| 全部图片体积 | 10.41 MB |
| 最大单张图片 | 641 KB |
| 生产构建体积 | 10.72 MB |
| 首页首屏请求 | 5 个资源、3 张图片 |
| 首页首屏传输 | 约 642 KB |
| 首页 LCP | 约 172 ms（本地环境） |

首屏只会加载文件夹动画所需的三张图片。瀑布流图片使用 `IntersectionObserver`，在用户接近图库时按需加载；滚入图库时，当前可视范围内的图片开始请求。

## 动效

- 点击火花：只在点击后绘制约 450 ms，动画结束即停止帧循环。
- 像素按钮：只在鼠标悬停或键盘聚焦期间运行。
- 文件夹：页面打开后只播放一次自动展开动画。
- ShapeGrid：首页可见时以低速绘制背景方格；离开首页或标签页隐藏时停止。
- 系统开启“减少动态效果”时，三种动效都会停用或缩短。

## 发布建议

图片总量适合直接放入 GitHub 和 Cloudflare 静态站点；没有单文件触及 GitHub 100 MB 上限。Cloudflare 将自动通过 CDN 缓存静态图片。若后续素材增长到数百张，建议在上传流程中生成 WebP/AVIF 多尺寸版本。
