# 本地私有资料

此文件夹用于保存不会上传到 GitHub 的本地资料。

每次影刀爬取完成后：

1. 将下载图片放入项目根目录的 `image` 文件夹。
2. 将最新表格命名为 `landscape.xlsx`，放入此文件夹。
3. 将 API 配置文件命名为 `API KEY.txt`，放入此文件夹；格式参考项目根目录的 `API配置示例.txt`。
4. 双击项目根目录的 `更新素材.cmd`。

AI 打标缓存、去重报告和删除记录也会自动保存在这里。请勿提交此文件夹中的其他文件。
